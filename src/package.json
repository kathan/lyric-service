{
    "name": "LyricService",
    "version": "0.0.1",
    "main": "index.js",
    "scripts": {
        "test": "jest --testPathPattern=^\\(\\(?!\\.aws-sam\\).\\)*$",
        "invoke": "sam local invoke \"LyricService\" --debug-port=5858 -e ./event.json",
        "build": "sam build",
        "debug": "sam local start-api -d 5858 -t ./.aws-sam/build/template.yml --debug",
        "start": "sam local start-api -t ./.aws-sam/build/template.yml"
    },
    "devDependencies": {
        "jest": "^26.4.2",
        "sequelize-mock": "^0.10.2"
    },
    "dependencies": {
        "bufferutil": "^4.0.1",
        "canvas": "^2.6.1",
        "pg": "^8.4.1",
        "sequelize": "^6.3.5",
        "utf-8-validate": "^5.0.2",
        "uuid": "^8.2.0"
    }
}
