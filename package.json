{
    "name": "LyricService",
    "version": "0.0.1",
    "main": "index.js",
    "scripts": {
        "test": "jest --testPathPattern=^\\(\\(?!\\.aws-sam\\).\\)*$",
        "build": "sam build -s ./src",
        "debug": "sam local start-api -d 5858 -t ./.aws-sam/build/template.yaml --debug --force-image-build --docker-network bridge",
        "start": "sam local start-api -t ./template.yaml"
    },
    "devDependencies": {
        "jest": "^26.4.2",
        "sequelize-mock": "^0.10.2"
    },
    "dependencies": {
        "bufferutil": "^4.0.1",
        "canvas": "^2.6.1",
        "pg": "^8.4.1",
        "sequelize": "^6.3.5",
        "utf-8-validate": "^5.0.2",
        "uuid": "^8.2.0"
    }
}
